# Backend environment variables
PORT=4000
LLM_API_KEY=
LLM_MODEL=llama-3.3-70b-versatile
# Frontend origin allowed to call this backend in production.
# Leave empty during local development (localhost is allowed automatically).
FRONTEND_URL=https://your-frontend-url.vercel.app
